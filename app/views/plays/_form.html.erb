<%= simple_form_for @play do |f| %>
  <%= f.input :title, label: "영화 제목" %>
  <%= f.input :description, label: "설명 및 후기" %>
  <%= f.input :director, label: "감독" %>
  <%= f.button :submit, class: 'btn btn-default' %>
  <%= link_to 'Cancel', :back, class: 'btn btn-default' %>
<% end %>
